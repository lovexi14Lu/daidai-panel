services:
  daidai-panel:
    build:
      context: .
      dockerfile: Dockerfile
    image: daidai-panel:latest
    container_name: daidai-panel
    restart: unless-stopped
    ports:
      - "5700:5700"
    volumes:
      - ./data:/app/data
    environment:
      - TZ=Asia/Shanghai
